name: my_pal
description: My Pal app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  di:
    path: ../di
  domain:
    path: ../domain
  storage:
    path: ../storage

  feature_record_list:
    path: ../features/record_list
  feature_home:
    path: ../features/home
  feature_settings:
    path: ../features/settings

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true